// Ej. 4. ¿Qu´e pasa cuando un proceso no libera su memoria (con free()) antes de terminar?

// Lo que pasa es que al perderse esa referencia a memoria, la memoria queda ocupada e inutilizable en el heap, hasta que termine el programa.

